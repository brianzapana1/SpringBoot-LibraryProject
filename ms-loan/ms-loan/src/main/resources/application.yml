server:
  port: 8083

spring:
  application:
    name: ms-loan
  config:
    import: optional:configserver:http://localhost:8888

  datasource:
        url: jdbc:mysql://localhost:3306/biblioteca_db?useSSL=false&serverTimezone=UTC
        username: root
        password: ""
  profiles:
    active: docker

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

